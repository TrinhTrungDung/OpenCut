---
description: ⚡⚡⚡⚡⚡ Bootstrap with parallel execution
argument-hint: [package-name]
---

**Ultrathink parallel** to bootstrap: <tasks>$ARGUMENTS</tasks>

**IMPORTANT:** Activate needed skills. Ensure token efficiency. Sacrifice grammar for concision.

## Workflow

### 1. Research
- Use `researcher` agents to find best practices
- Use `/scout:ext` to find existing patterns in monorepo

### 2. Parallel Planning
- Trigger `/plan:parallel <detailed-instruction>`
- Create phases with clear file ownership

### 3. Parallel Implementation
- Launch multiple `fullstack-developer` agents for independent phases
- Wait for completion before dependent phases

### 4. Testing & Review
- Use `tester` for test suite
- Use `code-reviewer` for all changes

### 5. Final Report
- Summary, guide, next steps
- Ask to commit (use `git-manager` if yes)
